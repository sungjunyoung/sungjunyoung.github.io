---
// A hairline at the very top of the viewport that fills as the article is
// scrolled. Rendered on every page; the script removes it where there is no
// article to measure (the post list, the tag pages, 404).
---

<div class="reading-progress" aria-hidden="true">
  <div class="reading-progress__bar"></div>
</div>
